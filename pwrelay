#!/usr/bin/env bash
# Thin shim for the Python pwrelay. Keeps the `./pwrelay` UX on Mac/Linux
# while the implementation lives in pwrelay.py and is shared with Windows
# (where users run `python pwrelay.py ...` or the included pwrelay.cmd).
exec python3 "$(dirname "$0")/pwrelay.py" "$@"
